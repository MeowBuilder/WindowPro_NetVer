맵 제작하는 경우
1. 클라 하나가 맵 제작 후 시작
2. 클라가 보낸 맵 초기 정보 서버가 받고 다시 모든 클라에 전송
3. 클라가 맵 정보 수신 후 게임 시작 
~~~

기본 맵 사용(지금 사용하는 기본 맵{init_map 함수 사용}을 서버에서 가져야 함) 하는 경우
1. 클라에서 게임 시작
2. 서버에서 가진 기존 맵을 모든 클라에 전송
3. 클라가 맵 수신 후 게임 시작
~~~

// 이거 2개 분기 나눠서 처리
// 클라에서 처음 보내는 StartSessionPaceket 수정해서 map number도 같이(제작이면 0, 기본
// 맵 쓸거면 3? 아무튼 2개 다르게)
// 이게 기본 맵인지, 제작한 맵에서 시작하는 게임인지 구분하고
// 서버는 보내준 맵 정보로 채우거나, 기본 맵으로 채운다.(init_map에 인자 0 넣어서)
// 그렇게 완성된 맵 정보를 모든 클라에 전송한다.

// 기본 맵일 경우 클라는 StartSessionPaceket 만 보낸다
// 제작한 맵일 경우 StartSessionPaceket 보낸 후 UploadMapPacket도 
// 보낸다.

// 서버는 StartSessionPacket의 내용 보고 알아서 구분해서 처리한다.

// 어떤 맵이던 간에 서버가 채운 후의 맵 구조를 MapInfoPacket에
// 담아서 모든 클라에 뿌리면 된다.

// 그 후에 게임 시작하고 GameStatePacket 로 전달하면서 동기화하고
// 게임 진행?

// 스테이지 클리어 시 서버에서 init_map 호출(map_num 인자 바꿈)
// 맵 정보 갱신한 후에 다시 MapInfoPacket 전달한다.


// 수정해야 하는 거
// 서버에서 init_map 쓸 수 있게
// 지금은 GetWindowRect(GetDesktopWindow(), &Desk_rect); 로 얻는 rect, Player*, map_num
// 전달해서 쓰게 되어 있음. rect는 고정 사이즈 쓴다 하고, Player만 바꾸면 될듯?


